package hadi.greencode.instapagedownloader.utils;

public class SharingIntents {

//    public static void store(Context context, Store store) {
//        String shareText = "نام فروشگاه : " + store.getStoreTitle() + '\n'
//            + "نام شهر : " + SHPManager.getInstance(context).getAll().getCity() + '\n'
//            + "http://didapp.ir/store/" + store.getStoreID();
//
//        Intent intent = new Intent(Intent.ACTION_SEND);
//        intent.setType("text/plain");
//        intent.putExtra(Intent.EXTRA_TEXT, shareText);
//        context.startActivity(Intent.createChooser(intent, context.getString(R.string.app_name)));
//    }
}
